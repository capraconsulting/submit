branch-defaults:
  master:
    environment: submit-prod
environment-defaults:
  submit-prod:
    branch: null
    repository: null
global:
  application_name: capracon-submit
  default_platform: Java 8
  default_region: eu-central-1
  profile: capra
  sc: git
deploy:
  artifact: app.zip
